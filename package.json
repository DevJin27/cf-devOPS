{
  "name": "cursor-devops",
  "version": "0.1.0",
  "private": true,
  "description": "Graph-first infrastructure copilot on Cloudflare Workers, D1, R2, Vectorize, and Workflows.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "test": "vitest run",
    "dev:api": "wrangler dev --config apps/api-worker/wrangler.jsonc",
    "deploy:api": "wrangler deploy --config apps/api-worker/wrangler.jsonc",
    "db:migrate:local": "wrangler d1 migrations apply cursor_devops --local --config apps/api-worker/wrangler.jsonc",
    "db:migrate:remote": "wrangler d1 migrations apply cursor_devops --remote --config apps/api-worker/wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.20.0"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  }
}
